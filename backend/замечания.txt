--pool=threads изучить для чего нужно
посмотреть что такое мнопоточность
ORM что это такое 
что такое middleware
CORSMiddleware??
app.mount("/uploads", StaticFiles(directory="uploads"), name="uploads")
apphet apppost
как взаимодействует фронт с бэеком : как отображается главная страница
SQLite и многопоточность
db.commit()
db.refresh(db_user)
create_access_token как работает где и как выполняется проверка
пагенация главной страницы def get_videos(
    skip: int = Query(0, ge=0),????
    limit: int = Query(50, ge=1, le=100),
    db: Session = Depends(get_db)
):
SQL PrimaryKEY FOREIGNKEY
Индексы БД 
me/videos Убрать дублирование